meta {
  name: get one with view code
  type: http
  seq: 2
}

get {
  url: {{PROTOCOL}}://{{URL}}/{{PREFIX}}/pasta/:slug
  body: json
  auth: none
}

params:path {
  slug: slug3
}

body:json {
  {
    "view_key": "view"
  }
}
